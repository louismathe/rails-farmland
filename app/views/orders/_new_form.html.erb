<div class="form">
  <%= simple_form_for @order do |f| %>
  <%= f.hidden_field :product_id, value: product.id %>
  <%= f.input :quantity %>
  <%= f.input :delivery_time %>
  <%= f.submit class: 'btn btn-lg btn-primary', submit_html: { value: 'toto' } %>
  <% end %>
</div>
